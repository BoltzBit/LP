#calculo da area do retangulo

lado1 = float(input('Insira o valor do primeiro lado:'))
lado2 = float(input('Insira o valor do segundo lado: '))

area = lado1*lado2

msg = 'A area do retangulo é {}'

print(msg.format(area))
